import { expect } from chai;

import api from './api';

const userApi = api.userApi;
const commentApi = api.commentApi;

describe('comment', async () => {
  describe('comment(id: ID!): Comment!', async() => {
    it('should return correct comment when queried', async() => {
        
    });
  });
});
